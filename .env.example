# Shared access code for the private /receipt treasurer tool.
# Required for the page to unlock. Also set this in Cloudflare Pages
# → Settings → Environment variables, then rebuild.
NEXT_PUBLIC_RECEIPT_ACCESS_CODE=
